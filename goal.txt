xcono/asr — standalone ASR/STT module for the realtime voice agent.

This is the "ears" of a voice pipeline that was decoupled out of xcono/voices
into three Go modules. This file is the canonical record of that decoupling: the
architecture, the resolved decisions, and the remaining work.

The service includes:
- silero-go VAD
- ASR/STT model interface (GigaAM local, ElevenLabs remote)
- an importable facade (Stream/Batch/IsSpeaking) — events over Go channels
- cmd/vox: a standalone debug binary (logs events to stdout; no transport)

Config: config.json — VAD timing, STT provider. No event-transport config.
Dev-ops: avoid Python where possible; GigaAM/Qwen3-ASR run as Python servers
(no Go equivalent), pointed at via base_url.
Development: built iteratively by AI coding agents.

# Decoupling

1. Facts
  - The voice pipeline is split into three Go modules, consumed in-process:
      xcono/asr    — STT "ears": VAD + transcribe (cgo/ONNX). THIS repo.
      xcono/tts    — TTS "mouth": synthesis client (pure-Go).
      xcono/voices — LLM brain (act, bus, llm) + the audio device.
  - voices imports asr + tts as library deps; streaming flows over Go channels /
    emit callbacks. NO event transport in the speech modules at all — if
    cross-service propagation is ever needed, voices (the central unit) owns a
    NATS server in its docker stack; the modules stay transport-free.
  - Python ASR servers moved here: llm/giga-asr -> asr/llm/giga3,
    llm/qwen3-asr -> asr/llm/qwen3. TTS server moved to tts/llm/qwen3-tts.
  - The old ./stt copy (module github.com/xcono/stt) was superseded by this repo
    (github.com/xcono/asr) and deleted; this repo carries its full history.

2. Resolved decisions
  - 2 module repos (asr, tts) + voices. Rationale: per-component focus for
    AI-driven dev, independent versioning, and asr's heavy cgo/ONNX/PortAudio
    deps stay out of the pure-Go tts client.
  - In-process Go modules, NOT network services. NATS removed entirely (2026-06-27):
    it was unneeded for high cohesion / low coupling. cmd/vox now logs to stdout.
    The central unit (voices) is where any future event bus would live.
  - voices knows ONE rule about STT: when VAD becomes TRUE, interrupt the TTS
    that is currently speaking (event-driven via SpeechStart, or IsSpeaking()).
  - Audio contracts (fixed): STT/VAD = 16 kHz/16-bit/LE/mono; TTS = 24 kHz/mono/int16 LE.
  - asr facade: Events() (Stream), Transcribe() (Batch), IsSpeaking() (atomic bool).
  - tts facade: Stream (SynthesizeStream), Batch (Synthesize), Status (ready/processing/speaking).
  - Shared pkg/audio is COPIED into each repo (no shared module yet).
  - go.work at the xcono/ parent (use ./asr ./tts ./voices) for local dev; voices
    also has local `replace` directives so plain `go build` works.

3. Status
  [x] asr facade (stt.go) — New/NewWith, Events/Transcribe/IsSpeaking.
  [x] NATS removed entirely (pkg/nats deleted, config.NATS gone, deps tidied);
      cmd/vox logs events to stdout. Module is transport-free.
  [x] tts module — Provider (Client/ElevenLabs/Fallback) + Service facade + cmd/say.
  [x] voices wired: cmd/act + run/* + cmd/{say,loopback,tts-record} on the modules;
      voices/pkg/{asr,vad,tts} deleted (voices = brain + audio device only).
  [x] go.work + voices go.mod require+replace for asr & tts.
  [x] Python server moves committed; the mis-copied tts/llm/qwen3-tts ASR payload
      was replaced with the real faster-qwen3-tts server (openai_server.py).
  [x] tts standalone debug binary (cmd/say, WAV out) + tts docker-compose.
  [x] voices docker-compose: default network named `voices`; tts/asr composes join it.
  [x] Refreshed asr README.md / AGENTS.md to a facade-first, transport-free framing.
  [x] Removed the duplicated voices/llm/{giga-asr,qwen3-asr,qwen3-tts}; voices/llm keeps only qwen3-llm.
      voices compose now builds stt/tts from ../asr and ../tts (single source); all composes validate.
  [x] asr docker-compose.yaml (GigaAM stt:8008), parity with tts; both join the `voices` network.

Decoupling complete. voices is a focused LLM codebase consuming asr + tts as
in-process Go modules; each module is standalone-runnable with its own compose +
debug binary. All four compose configs validate (docker compose config).
